# COIN-FLIP-SIMULATOR
This project simulates a coin flip by using a flipCoin() function. The function generates a random result with a 50-50 chance of returning either 'heads' or 'tails'. Using a separate function makes the code cleaner, easier to read, and more reusable.
